# 工具列表漂移

MiMo for Copilot Chat 检测到当前会话中的工具列表在不同轮次之间可能不稳定。

这是因为 `tools` 数组本身也是请求前缀的一部分。如果工具列表频繁变化，上下文缓存命中率会下降，请求体也可能变大。

## 建议处理方式

1. 打开 VS Code 的工具配置，关闭不常用工具。
2. 如果预激活工具带来了额外干扰，请关闭 `mimo-copilot.experimental.stabilizeToolList`。
3. 尽量在同一条对话里保持启用工具集合稳定且精简。
